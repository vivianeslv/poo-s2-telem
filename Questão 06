/*6. Desconto Aplicado
Crie um programa que calcula o preÃ§o com desconto. Receba o preÃ§o original e o percentual de desconto (ex: 15%). Use o operador de multiplicaÃ§Ã£o * e subtraÃ§Ã£o - para calcular o valor final.
PeÃ§a um nÃºmero ao usuÃ¡rio e determine se ele Ã© par ou Ã­mpar. Use o operador de mÃ³dulo % para verificar o resto da divisÃ£o por 2.*/

package Questoes;
import java.util.Scanner;
public class Desconto {
	public static void main (String [] args ){
		Scanner scanner = new Scanner(System.in);
		System.out.println("Digite o preço: ");
		double preco = scanner.nextDouble();
		System.out.println("Digite o desconto: ");
		double desconto = scanner.nextDouble();
		double calculodesc = preco * (desconto/100);
		System.out.println("O desconto é de R$ " + calculodesc);
		double precofinal = (preco - calculodesc);
		System.out.println("O preço final com desconto é R$ " + precofinal);
		
	}
}
